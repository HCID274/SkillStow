# 给 skillstow 的贡献者（人和 AI）

## 动手前先读这三条

这三条是**硬护栏**，不是建议。上一版项目 13 天写了 80,886 行 Rust 来同步 916 KB 的
markdown，最后 30 多个提交全在修 Windows 文件系统问题，核心闭环一次都没跑通。
那次失败的每一步都是从违反下面某一条开始的。

**一、禁止自建文件系统抽象层。**
文件操作直接用 `std::fs`。出错就 `return Err(...)`。
不建事务层、不做原子替换、不写重试、不碰 NT 路径、不处理 reparse point 的二进制结构。
上一版的 `crates/safe-fs` 有 3,273 行，没有任何人要求它存在，它是自愿写的。

**二、禁止 libgit2 / gitoxide。**
所有 git 操作 shell out 给 `git` 命令，读它的 stdout/stderr 和退出码。
git 自己已经在三个平台上解决了所有这些问题，重新实现一遍只会重新踩一遍。

**三、确定性代码 1000 行硬上限（不含测试）。**
这不是质量目标，是**绊线**。
`tokei src/` 或 `find src -name '*.rs' | xargs wc -l` 超过 1000 的那一刻，
不管手头的功能多合理，都停下来重读 `docs/REPLAN.md`，问一句：
**我是不是又在解决一个不存在的问题？**

预算分配见 `docs/SPEC.md` 第 6 节。预算已经排满，加任何新东西都得先删掉等量的旧东西。

## 文档不冻结

`docs/SPEC.md` 是行为契约，不是圣旨。你在实现中发现它错了、含糊、或者代价不成比例，
**直接改它**，在 PR 里说明改了哪条、为什么。

上一版有一份 656 行的"frozen 蓝图"，规定"冻结后 Worker 不得自行修改公共协议"。
结果是没人敢改，只能绕，绕出来的东西越堆越多。这次不重复。

唯一不能自行改的是上面那三条护栏和 `docs/REPLAN.md` 里的 28 条决策——
那些要改先跟项目所有者谈。

## 完成的定义

见 `docs/REPLAN.md` 的 D20。在那条验收跑通之前，
**不写 CI、不写安装包、不写 README、不写 LICENSE、不发布、不接第三台机器、不写 adapt。**
一样都不许提前。
