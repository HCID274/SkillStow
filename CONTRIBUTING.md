# 开发约定

先读 AGENTS.md、docs/HANDOFF.md 和 docs/SPEC.md。最新范围是四端统一精简模块，旧的三端副本与暂缓 Windows 已结束。

- 优先减少内容和机制；只保留明确需要的功能。实质歧义先对齐，普通实现细节自主完成。
- 使用系统 Git 和标准库；不重建 Git 协议，不恢复 Hub、审批平台、LRU 或遥测。
- plan.rs 负责纯计划，link.rs 集中平台链接行为，migration/skill_module.py 负责此私人模块。回滚只覆盖本次触达文件；不递归删除目录链接目标。
- Rust 规模超过 1000 行时审视职责，不能靠压行或移动通用代码绕过。当前增加来自单用途适配器入口、Windows 路径兼容和免重复推送，没有引入新的同步引擎。
- 对照实际 diff 选择验证；跨平台变更必须在 Windows 原生和 Linux 实机检验。已有无变化的验证不机械重跑。
- 代码和文档按能力使用中文 Conventional Commits；保护既有修改，不强推。
- 代码提交/push、内容发布、设备安装及客户端发现分别记录。设备已应用不代表已有会话重载。
