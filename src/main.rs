//! T5 — 命令入口（SPEC 第 4、5 节），预算 120 行。
//!
//! 动手前先读 CONTRIBUTING.md 的三条硬护栏和 docs/SPEC.md。

mod cmd;
mod config;
mod link;
mod plan;
mod repo;

fn main() {
    eprintln!("skillstow 尚未实现，见 docs/TASKS.md");
    std::process::exit(2);
}
