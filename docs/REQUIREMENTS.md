# 已确认需求

更新：2026-09-23。

- 范围是 system/manifest.json 登记的设备：Mac、Windows 原生账户、HomeServer、Shenzhen、plasma-fes。新增设备在清单登记，不另建流程。
- 整个 Skills 系统作为一个统一模块维护；任意端可修改并发布，离线端恢复后补齐。
- 共用内容只有一份规范源；设备专属内容也存于同一私人仓，各端完整 checkout，只应用适用内容。
- 项目专属 Skills 同样存于私人仓，只在登记的设备与项目目录中生效，不进入全局目录；项目路径随设备登记。
- 程序读清单定位 Skill、确定设备范围、同步并汇总收据；AI 只读取和修改相关内容。
- 整体优先删减：宁可多删并在实际需要时补回，不保留历史包袱；程序也只保留实际使用的一条路径。
- 不恢复多层路由、LRU、调用统计、重复治理与角色流程。
- 目标、范围、成功标准、授权或关键取舍不清楚时，先停下依赖工作，复述并对齐后继续。
- 编辑期间不发布半成品；沿用 Git main、持久编辑标记、宿主定时器及冲突现场。
- 凭据、缓存、状态和插件留在本地。退役内容可从 Git 历史或迁移备份恢复，但不继续向模型暴露。
- 验收区分代码发布、内容发布、设备应用和真实客户端发现；不声称旧任务已自动重载。
