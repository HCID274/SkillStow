# 已确认需求

更新：2026-09-09。

- 范围只包括 Tailscale 中的 Mac、Windows、HomeServer、Shenzhen；Windows 指当前原生客户端环境，WSL 是可另行登记的安装目标。
- 整个四端 Skills 系统作为一个统一模块维护；任意端可修改并发布，离线端恢复后补齐。
- 所有共用内容只有一份规范源。设备专属内容也存于同一个私人仓，各端完整 checkout；程序只把适用内容应用给本端客户端。
- 程序读清单定位 Skill、确定设备范围、同步并汇总收据；AI 只读取和修改相关内容。新 Skill 或实质范围变化首次确定归属，日常不逐台重新调查。
- 整体优先删减：宁可多删并在实际需要时补回，也不保留历史包袱。Windows 旧内容不是保留基线；Mac 既有机制也接受精简。
- 退出旧的多层路由、LRU 升温、调用统计、重复治理与角色流程。使用少量职责清楚的原生 Skill 入口。
- 目标、范围、成功标准、授权或关键取舍不清楚时，先停下依赖工作，复述并对齐后继续。
- 编辑期间不发布半成品；沿用 Git main、持久编辑标记、宿主定时器及冲突现场。
- 凭据、缓存、状态和插件留在本地。退役内容可从 Git 历史或迁移备份恢复，但不继续向模型暴露。
- 验收区分代码发布、内容发布、设备应用和真实客户端发现；不声称旧任务已自动重载。

2026-09-06 的三套设备副本及暂缓 Windows 是历史阶段，已由以上范围替代。
